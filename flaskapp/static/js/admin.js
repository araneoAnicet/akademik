import Vue from 'https://cdn.jsdelivr.net/npm/vue@2.6.10/dist/vue.esm.browser.js'

var app = new Vue({
    delimiters: ['[[', ']]'],
    el: '#app',
    data: {
        user_requests: [],
    },
    methods: {
        'days_reqyests': function days(){
            
        }
    }
})
